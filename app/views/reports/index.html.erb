<div class="container mt-5">
  <h1 class="mb-4 text-center textcolor">Relèves</h1>

  <div class="scroll_report_index">
    <table class="table table-striped table-hover align-middle">
      <thead class="table-dark">
        <tr>
          <th class="text-center">Période</th>
        </tr>
      </thead>

      <tbody>
        <% @reports.each do |report| %>
          <tr>
            <td class="text-center">
              <%= link_to "Du #{report.start_date.strftime('%d/%m/%Y')} au #{report.end_date.strftime('%d/%m/%Y')}", report_path(report), class: "text-decoration-none text-dark fw-bold" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
      <div class="text-center mt-2 mb-4">
    <%= link_to "+", new_report_path, class: "btn button-67 p-2" %>
  </div>
</div>
