<h1>Pathologies#new</h1>
<p>Find me in app/views/pathologies/new.html.erb</p>


<%= simple_form_for([@patient, @note], url: patient_notes_path(@patient)) do |f| %>
  <%= f.input :text, label: "Texte de la note" %>
  <%= f.button :submit, "Créer la note" %>
<% end %>

<%= link_to 'Retour au patient', patient_path(@patient) %>
